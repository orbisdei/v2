# Copy to .env and fill in — same Supabase project as the web app.
# EXPO_PUBLIC_ vars are embedded in the client bundle (anon key is safe to expose; RLS protects data).
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
